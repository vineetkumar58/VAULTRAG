// Fill this in after `terraform apply` - run `terraform output api_base_url`
// and paste it here. This is the ONLY required manual edit to run the frontend.
const CONFIG = {
  API_BASE_URL: "https://YOUR-API-ID.execute-api.YOUR-REGION.amazonaws.com",
};
